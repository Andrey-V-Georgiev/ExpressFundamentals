<div class="spacer">
    <h2>{{article.title}}
        <div class="controls">
            {{#if currentUser}}
            <a href="/edit/{{article._id}}">[edit]</a>
            <a href="/history/{{article._id}}">[history]</a>
            {{/if}}
        </div>
    </h2>
    <article>
            <img src="{{lastEdit.imageUrl}}" >
            <p>
                {{lastEdit.content}}
            </p>

    </article>
</div>